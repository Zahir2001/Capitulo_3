package Ej3_14;

public class BackupEj {
    
    // PREGUNTA
    /* Dé tres ejemplos diferentes de una sola sentencia Java que asigne la variablebackup,
     * a un nuevo array con copias de todas las entradas int de un array existente,original.
     */

    // RESPUESTA
    // backup = originial.clone;

    // backup = java.util.Array.copyOf(original, backup.lenght);

    // backup = java.util.Array.copyOfRange(original, 0, backup.lenght);
}
